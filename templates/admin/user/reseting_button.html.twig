{% if admin.canAccessObject('edit', object) and admin.hasRoute('edit') %}
    <li>
        <a class="sonata-action-element" href="{{ path('admin_password_reset_for_user', {'user': object.id }) }}">
            <i class="fa fa-unlock-alt" aria-hidden="true"></i>
            {{ 'user.action.reset_password.button'|trans }}
        </a>
    </li>
{% endif %}
